<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OLS Viikkopelit - Hallinta</title>
    <link rel="stylesheet" href="/css/style.css"> <!- Output from Tailwind ->
</head>
<body class="bg-gray-100 text-gray-800 p-8">
    <div class="max-w-2xl mx-auto bg-white p-6 rounded-lg shadow-md">
        <h1 class="text-2xl font-bold mb-6 text-center"><a href="/" class="text-gray-800 hover:text-blue-600 transition-colors duration-150">OLS Viikkopelit - Hallintapaneeli</a></h1>

        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-4">Päivitä Pelitiedot</h2>

            <!-- Tab Buttons -->
            <div class="mb-4 border-b border-gray-200">
                <nav class="-mb-px flex space-x-8" aria-label="Tabs">
                    <button type="button"
                            class="tab-button whitespace-nowrap py-3 px-1 border-b-2 font-medium text-sm text-blue-600 border-blue-500"
                            data-tab-target="#automaticUpdateTab" aria-current="page">
                        Automaattinen Päivitys
                    </button>
                    <button type="button"
                            class="tab-button whitespace-nowrap py-3 px-1 border-b-2 font-medium text-sm text-gray-500 hover:text-gray-700 hover:border-gray-300 border-transparent"
                            data-tab-target="#manualUploadTab">
                        Manuaalinen Tuonti
                    </button>
                </nav>
            </div>

            <!-- Status Message Display -->
            <% if (locals.message && locals.messageType) { %>
                <div class="my-4 p-4 rounded-md 
                            <% if (messageType === 'error') { %> 
                                bg-red-100 border border-red-400 text-red-700 
                            <% } else if (messageType === 'success') { %> 
                                bg-green-100 border border-green-400 text-green-700 
                            <% } else { %> 
                                bg-blue-100 border border-blue-400 text-blue-700 
                            <% } %>"
                      role="alert">
                    <p class="font-semibold">Tila:</p>
                    <p><%- message %></p>
                </div>
            <% } %>

            <!-- Tab Content Panels -->
            <div>
                <!-- Automatic Update Tab Content -->
                <div id="automaticUpdateTab" class="tab-content">
                    <p class="text-gray-600 mb-4">
                        Hae uusin otteluohjelma PDF-muodossa OLS:n sivuilta, käsittele se ja päivitä tallennetut pelitiedot.
                    </p>
                    <form action="/trigger-full-update" method="POST">
                        <div class="mb-4 flex items-center">
                            <input type="checkbox" id="forceUpdate" name="forceUpdate" class="mr-2 h-4 w-4 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
                            <label for="forceUpdate" class="text-sm text-gray-700">
                                Pakota päivitys (käsittele nykyinen PDF uudelleen, vaikka päivämäärä ei olisi muuttunut)
                            </label>
                        </div>
                        <button type="submit"
                                class="w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded focus:outline-none focus:shadow-outline transition duration-150 ease-in-out">
                            Päivitä pelit
                        </button>
                    </form>
                </div>

                <!-- Manual Upload Tab Content (Initially Hidden) -->
                <div id="manualUploadTab" class="tab-content hidden">
                     <p class="text-gray-600 mb-4">
                        Jos automaattinen tietojen haku epäonnistuu tai sinun tarvitsee ladata tietty PDF-tiedosto, käytä tätä lomaketta. Vaatii salasanan.
                    </p>
                    <form action="/admin/upload-pdf" method="POST" enctype="multipart/form-data">
                        <div class="mb-4">
                            <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Salasana:</label>
                            <input type="password" id="password" name="password" required
                                   class="mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm">
                        </div>

                        <div class="mb-6">
                            <label for="pdfFile" class="block text-sm font-medium text-gray-700 mb-1">PDF-tiedosto:</label>
                            <input type="file" id="pdfFile" name="pdfFile" accept=".pdf" required
                                   class="mt-1 block w-full text-sm text-gray-900 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none
                                          file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0
                                          file:text-sm file:font-bold file:bg-blue-500 file:text-white
                                          hover:file:bg-blue-700 file:transition file:duration-150 file:ease-in-out">
                        </div>

                        <button type="submit"
                                class="w-full bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-4 rounded focus:outline-none focus:shadow-outline transition duration-150 ease-in-out">
                            Lataa ja Käsittele PDF
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="mb-8">
            <h2 class="text-xl font-semibold mb-3">Tarkista Pelitiedot</h2>
            <p class="text-gray-600 mb-4">
                Näytä yksityiskohtainen yhteenveto kaikista poimituista pelitiedoista kentittäin järjestettynä. Tämä näkymä auttaa tunnistamaan puuttuvat pelit, aikatauluristiriidat ja muut mahdolliset ongelmat tiedoissa.
            </p>
            <a href="/check-data" 
               class="block w-full bg-green-500 hover:bg-green-700 text-white font-bold py-3 px-4 rounded focus:outline-none focus:shadow-outline transition duration-150 ease-in-out text-center">
                Näytä Tietojen Yhteenveto
            </a>
        </div>


        <div class="mt-8 text-center">
            <p class="text-sm text-gray-500">
                Pääsovellus (ols-viikkopelit) lukee päivitetyt tiedot jaetusta tallennustilasta. 
                Sinun saattaa tarvita käyttää sen 'Päivitä Tiedot' -toimintoa nähdäksesi muutokset välittömästi.
            </p>
        </div>
    </div>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const tabButtons = document.querySelectorAll('.tab-button');
        const tabContents = document.querySelectorAll('.tab-content');

        tabButtons.forEach(button => {
            button.addEventListener('click', () => {
                const targetTab = button.dataset.tabTarget;

                // Deactivate all buttons and hide all content
                tabButtons.forEach(btn => {
                    btn.classList.remove('text-blue-600', 'border-blue-500');
                    btn.classList.add('text-gray-500', 'hover:text-gray-700', 'hover:border-gray-300', 'border-transparent');
                    btn.removeAttribute('aria-current');
                });
                tabContents.forEach(content => {
                    content.classList.add('hidden');
                });

                // Activate clicked button and show its content
                button.classList.add('text-blue-600', 'border-blue-500');
                button.classList.remove('text-gray-500', 'hover:text-gray-700', 'hover:border-gray-300', 'border-transparent');
                button.setAttribute('aria-current', 'page');
                
                if (targetTab) {
                    const activeContent = document.querySelector(targetTab);
                    if (activeContent) {
                        activeContent.classList.remove('hidden');
                    }
                }
            });
        });
    });
</script>
</body>
</html>
