# 🚀 OPTIMIZED .dockerignore - Exclude unnecessary files for minimal build context

# Dependencies (installed in container)
node_modules/
npm-debug.log*

# Build outputs (built inside container)
dist/

# Development files
*.log
*.env.local
*.env.development
.env.test
.env.production

# Version control
.git/
.gitignore

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Test files
test/
tests/
*.test.js
*.test.ts
coverage/

# Documentation (not needed in runtime)
README.md
PERFORMANCE_ANALYSIS.md
plan.md
docs/

# Scripts (keep essential ones but exclude dev scripts)
set-always-on.sh
set-sleep-mode.sh

# Fly.io configs (not needed in container)
fly.toml

# Admin app (separate container)
admin_app/

# Large PDF files (should be in mounted volumes)
**/*.pdf
*.pdf
persistent_app_files/downloaded_pdfs/

# Persistent app files (mounted volumes in production)
persistent_app_files/
admin_app/persistent_app_files/

# Large development assets that aren't needed in production
public/images/*.png
public/images/*-original.*

# Temporary files
tmp/
temp/

# Build artifacts that get regenerated
parsed_pdf_data.json

# Docker files (prevent recursion)
Dockerfile*
.dockerignore