<!DOCTYPE html>
<html lang="fi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= baseTeamName %> joukkueportaali - katsele alijoukkueiden aikatauluja ja pelejä.">
    <meta property="og:title" content="<%= documentTitle %>">
    <meta property="og:description" content="<% if (!loading && subteams.length > 0) { %><%= subteams.map(st => st.nextGame ? `${st.name.split(' ').pop()} - ${st.nextGame.time}` : `${st.name.split(' ').pop()} - Ei pelejä`).join(', ') %><% } else { %>Joukkueportaali - katsele alijoukkueiden aikatauluja<% } %>">
    <meta property="og:type" content="website">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <title><%= documentTitle %></title>

    <!-- Tailwind CSS -->
    <link href="/css/style.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-br from-slate-50 to-slate-100 min-h-screen flex flex-col">
    <div class="container mx-auto max-w-4xl px-4 py-8 flex-grow">
        <header class="bg-gradient-to-r from-blue-600 to-blue-800 text-white p-6 rounded-2xl shadow-2xl mb-8">
            <h1 class="text-3xl md:text-4xl font-bold text-center mb-2">
                <%= baseTeamName %>
            </h1>
            <p class="text-center text-blue-100 text-lg">Joukkueportaali</p>
        </header>

        <main class="flex-grow">
            <!-- Back navigation -->
            <div class="mb-6">
                <a href="/" class="inline-flex items-center text-blue-600 hover:text-blue-800 font-medium transition-colors">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
                    </svg>
                    Takaisin kaikkiin joukkueisiin
                </a>
            </div>

            <% if (typeof loading !== 'undefined' && loading) { %>
                <div class="text-center py-12">
                    <div class="inline-block animate-spin rounded-full h-12 w-12 border-4 border-blue-600 border-t-transparent"></div>
                    <p class="mt-4 text-xl text-blue-800 font-semibold">Ladataan alijoukkueita...</p>
                    <p class="text-sm text-slate-600 mt-2">Palvelin käynnistyy, odota hetki.</p>
                </div>
            <% } else if (subteams.length > 0) { %>
                <h2 class="text-2xl font-bold text-slate-800 mb-6">
                    Alijoukkueet (<%= subteams.length %>)
                </h2>

                <div class="grid gap-6 md:grid-cols-2">
                    <% subteams.forEach((subteam) => { %>
                        <div class="bg-white p-6 rounded-xl shadow-lg border-2 border-blue-100 hover:shadow-xl hover:border-blue-300 transition-all duration-300">
                            <div class="flex justify-between items-start mb-4">
                                <h3 class="text-xl font-bold text-blue-700">
                                    <%= subteam.name.split(' ').pop() %>
                                </h3>
                            </div>

                            <% if (subteam.nextGame) { %>
                                <div class="mb-4 space-y-2">
                                    <p class="text-sm font-semibold text-slate-600 uppercase tracking-wide">
                                        Seuraava peli:
                                    </p>
                                    <div class="bg-blue-50 p-3 rounded-lg">
                                        <p class="text-lg font-bold text-slate-800 mb-1">
                                            <%= subteam.nextGame.time %>
                                        </p>
                                        <p class="text-md text-slate-700">
                                            vs <%= subteam.nextGame.opponent %>
                                        </p>
                                        <p class="text-sm text-slate-600 mt-2">
                                            📍 <%= subteam.nextGame.field %>
                                        </p>
                                        <% if (subteam.nextGame.location) { %>
                                        <p class="text-sm text-slate-600">
                                            🏟️ <%= subteam.nextGame.location %>
                                        </p>
                                        <% } %>
                                    </div>
                                </div>

                                <div class="text-xs text-slate-500 mb-4 py-3 border-t border-slate-200">
                                    <div class="flex flex-wrap gap-3">
                                        <span><span class="font-semibold">Pelimuoto:</span> <%= subteam.nextGame.gameType %></span>
                                        <span><span class="font-semibold">Kesto:</span> <%= subteam.nextGame.gameDuration %></span>
                                    </div>
                                </div>
                            <% } else { %>
                                <div class="mb-4 bg-slate-50 p-4 rounded-lg">
                                    <p class="text-slate-500">
                                        Ei tulevia pelejä
                                    </p>
                                </div>
                            <% } %>

                            <div class="pt-4 border-t border-slate-200">
                                <a href="/team/<%= encodeURIComponent(subteam.name) %>"
                                   class="block w-full text-center bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2.5 px-4 rounded-lg transition-colors duration-200">
                                    Näytä koko aikataulu
                                </a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="text-center py-12 bg-white rounded-xl shadow-lg">
                    <p class="text-slate-500 text-lg">Ei alijoukkueita löytynyt.</p>
                </div>
            <% } %>
        </main>

        <footer class="mt-12 text-center text-slate-600 text-sm py-6">
            <p>Talviliiga Tournament Viewer</p>
        </footer>
    </div>
</body>

</html>
